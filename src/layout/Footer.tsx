import AdsenseAd from "@/elements/ads/AdsenseAd";
import { Flex } from "@mantine/core";

const Footer = () => {
  return (
    <Flex bottom={0}>
      <AdsenseAd />
    </Flex>
  );
};

export default Footer;
